CREATE NEW CUSTOM NETWORK
docker network create redis


LAUNCH REDIS CONTAINER
docker run \
    --network redis \
    --name redis \
    -d redis


LAUNCH REDIS-COMMANDER CONTAINER
docker run \
    --network redis-commander \
    --name redis \
    -p 8081:8081 \
    -e REDIS_HOST=redis \
    -d rediscommander/redis-commander